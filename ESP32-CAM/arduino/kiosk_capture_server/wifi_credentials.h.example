// Copy this file to wifi_credentials.h (same folder) and fill in the real
// values before flashing. wifi_credentials.h itself is gitignored — never
// commit your actual WiFi password.
#pragma once
const char* ssid = "YOUR_WIFI_NAME";
const char* password = "YOUR_WIFI_PASSWORD";
