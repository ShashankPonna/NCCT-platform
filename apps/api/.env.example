PORT=4000
SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SERVICE_ROLE_KEY=
PUBLIC_WEB_URL=http://localhost:5173
# Required for F7's chatbot answer generation (docs/DECISIONS.md #17).
# Embeddings run locally and need no key; only the Claude call needs this.
ANTHROPIC_API_KEY=
